return {
  -- 管理器自身配置
  --defaults = { lazy = true },  -- 启用懒加载
  --install = { colorscheme = "tokyonight" },  -- 安装后自动设置主题
  --performance = {
    --rtp = {
      --disabled_plugins = { "netrw" }  -- 禁用内置插件
    --}
  --}
}
